<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.bussines.v1.qualimax">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".data.gerenciaLista.GerenciaDadosPonto"></activity>
        <activity android:name=".data.VerDados" />
        <activity android:name=".data.gerenciaLista.GerenciaDadosMaquina" />
        <activity android:name=".data.CadastrarMaquina" />
        <activity android:name=".data.CadastrarPonto" />
        <activity android:name=".data.PosLogin" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>